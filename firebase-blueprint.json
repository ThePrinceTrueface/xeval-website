{
  "entities": {
    "Comment": {
      "title": "Comment",
      "description": "User feedback or comment on xeval.js",
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "description": "The comment text contents"
        },
        "userId": {
          "type": "string",
          "description": "The UID of the user who posted the comment"
        },
        "userName": {
          "type": "string",
          "description": "The display name of the user"
        },
        "userPhoto": {
          "type": "string",
          "description": "The avatar URL of the user"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the comment was created"
        }
      },
      "required": ["text", "userId", "userName", "createdAt"]
    }
  },
  "firestore": {
    "comments/{commentId}": {
      "schema": "Comment",
      "description": "Global collection of user comments"
    }
  }
}
